# 다른 PC 수집 분담표

작성: 2026-09-21 18:40, 메인 PC(노트북, 경매서버 4011이 도는 곳)의 Claude. 숫자는 모두 이 시각 Supabase 실측이다.
이 문서가 **누가 무엇을 수집하는지의 기준**이다. 새 작업을 시작하거나 넘길 때는 여기 표를 먼저 고치고 push한다.

## 1. 원칙: 한 가지 일은 한 PC만

두 PC는 같은 Supabase·같은 R2·같은 API 키를 쓴다. 같은 일을 둘이 돌리면 아래 문제가 생긴다.

- 같은 행을 동시에 고쳐 교착(deadlock)이 난다. 실제로 9/18 목록 컬럼 동기화에서 겪었다.
- data.go.kr(건축물대장·K-apt)과 온비드 **일일 호출 한도**를 둘이 나눠 먹는다. 한도가 소진되면 그날 세대수·승강기가 빈칸이 된다.
- 법원경매 사이트는 봇을 감지한다(ipcheck). 두 곳에서 긁으면 차단 위험이 커진다.

## 2. 메인 PC가 하는 일 (다른 PC는 손대지 말 것)

| 일 | 어디서 | 비고 |
|---|---|---|
| 경매서버 4011 + 내부 반복작업 전부 | `api/main.py` (스케줄러 `JH옥션_서버4011`) | 기일현황 정규화 20분, 목록 컬럼 동기화 20분, 명세서→임차인 20분, 추정시세, 반경지도 좌표, **준공·세대 채우기 루프(`_brief_missing_loop`, 9/21 가동, 약 3,500건 처리 중)** |
| 법원경매 직접수집 | `auction_analysis/courtauction_collect_all.js` (`CourtCollectAll`, 2시간마다) | (신)스피드옥션 크롤러 세션 소관 |
| 서류·사진 수집 | 크롤러 세션 | 오늘 18:11까지 사진 2,553·명세서 1,100·감정평가서 271건 저장 중. **겹치지 말 것** |
| 공매(온비드) 수집·예열 | `crawl_gongmae.py`, `JH옥션_공매*` 5개 | |
| 엔카 수집·시세 | `JH엔카_*` | |
| 홈 뉴스, 지도좌표 동기화, 매각좌표, 자동예열(17:00), 캐시 Supabase 동기화 | `JH옥션_*`, `JH캐시_*` | |
| 다른 프로젝트 | Airbnb 5개, 당근 2개, 호스텔 예열 | 경매와 무관, 그대로 둔다 |

## 3. 다른 PC에 맡길 일

### A. KB부동산 매물 수집 (지금 완전히 멈춤, 최우선)

- **현상:** `kb_listing` 210,454건이 **전부 비활성(is_active=false)**. 8/22에 로그인 토큰이 만료된 뒤 수집이 안 되고 있다. 그래서 경매 목록의 "호가 N건"과 상세의 경쟁매물이 옛 값이다.
- **메인 PC에서 못 하는 이유:** KB 재로그인(카카오)은 사람이 해야 한다. 로그인한 그 PC에서 토큰을 받아 돌리는 게 가장 간단하다.
- **준비 (주인님):**
  1. kbland.kr에 카카오로 로그인한다.
  2. F12 콘솔에서 `JSON.parse(localStorage.vuex).member.refreshToken`을 실행한다.
  3. 나온 값을 다른 PC `.env`의 `KB_REFRESH_TOKEN`에 넣는다.
  - 토큰·비밀번호는 Claude가 다루지 않는다.
- **소량 확인:** `python kb_crawler.py --collect`를 한 번 실행한다. 로그에 `auth_failed`가 없어야 한다. 아래 쿼리로 활성 매물이 0에서 늘어나는지 본다.
  ```sql
  select is_active, count(*), max(updated_at) from kb_listing group by 1;
  ```
- **정기 실행:** 메인 PC와 같은 주기로 등록한다.
  - `--collect`: 매일 05:00
  - `--refresh-all`: 2주마다 일요일 21:00
  - `--gongmae`: 2주마다 일요일 20:00
- **넘겨받는 순간:** 메인 PC의 `JH옥션_KB매물수집`·`KB_Refresh_All`·`KB_Gongmae_Biweekly` 3개를 **메인 PC Claude가 끈다.** 다른 PC에서 첫 수집이 성공한 걸 확인한 뒤에 끈다. 둘이 동시에 돌지 않게 하기 위해서다.

### B. (선택, 주인님 결정 필요) 준공·세대 계산 나눠 돌리기

- **현상:** 준공·세대 기록이 없거나 빈 물건 약 3,500건을 메인 PC가 처리 중이다. 끝나기까지 5~6시간으로 추정한다.
- **나누려면:** 다른 PC가 **자기 data.go.kr 인증키**를 따로 가져야 한다. 같은 키면 한도만 나눠 먹어서 빨라지지 않는다.
- **코드도 바꿔야 한다:** 물건키를 둘로 나눠 한쪽만 계산하게 해야 하는데, 이 코드는 아직 없다. 결정이 나면 메인 PC Claude가 만든다.

## 4. 다른 PC에서 하면 안 되는 것

- 경매서버를 **로컬 모드로 띄우지 말 것.** 띄우면 2절의 20분 반복작업이 두 벌로 돈다. 꼭 띄워야 하면 `.env`에 `CLOUD_READER=1`을 넣는다(읽기 전용, 계산 안 함).
- `CourtCollectAll`, `courtauction_collect_all.js`, 스피드옥션·마이옥션 크롤러를 돌리지 말 것. 크롤러 세션 소관이다.
- `/admin/brief_recompute`, `/admin/schedule_normalize`, `/admin/apt_recompute` 같은 대량 재계산을 부르지 말 것.
- 2절 표에 있는 예약작업을 같이 등록하지 말 것.

## 5. 설치 (A만 할 경우)

```bash
git clone https://github.com/ketoup55-star/jh-auction.git
```
```bash
pip install -r requirements.txt
```
- `.env`에 필요한 키 **이름**: `SUPABASE_DB_URL`, `SUPABASE_SERVICE_KEY`, `KB_REFRESH_TOKEN` (필요하면 `KB_EMAIL`, `KB_PW`)
- 값은 주인님이 메인 PC `.env`에서 직접 옮긴다. 이 문서에는 적지 않는다.
- 파이썬은 메인 PC와 같은 3.14. `KB_Gongmae_Biweekly`만 3.13으로 등록돼 있다.

## 6. 메인 PC 쪽 확인 필요 (다른 PC와 무관, 메인 PC Claude가 처리)

- 오늘 12:24:47(재부팅 직후)에 예약작업 12개가 결과코드 0x800710E0("요청 거부")로 **실행되지 않았다.** KB수집, 당근, 엔카시세, 공매예열 4개, 캐시동기화, 지도좌표, Airbnb 2개 등이다.
  - 원인은 아직 모른다. 이벤트 로그를 못 읽었다.
  - 다음 정기 실행은 내일 새벽이다. 그때 정상으로 도는지 확인한다.
- `JH옥션_공매수집`은 오늘 결과 2(오류)로 끝났다. 원인 미확인.
